ALGORITHME Test1
constante:

variable:
	a : entier
	b : entier
	tmp : entier
	
DEBUT
	a <-- 1
	b <-- 10

	écrire(a)
	écrire(b)
	
	tmp <-- a
	a <-- b
	b <-- tmp
	
	si 1 < 2
	et 8 > 10 alors
		écrire("mdr")
		ecrire("mdr2")
	sinon
		ecrire("mdr3")
	fsi
	
	écrire(a)
	écrire(b)
	
FIN 