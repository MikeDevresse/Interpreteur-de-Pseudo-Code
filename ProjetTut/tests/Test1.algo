ALGORITHME Test1
constante:

variable:
	a : entier
	b : entier
	tmp : entier
	
DEBUT
	a <-- 1
	b <-- 10

	écrire(a)
	écrire(b)
	
	tmp <-- a
	a <-- b
	b <-- tmp
	
	si a < 2 
	et 15 < 20
	et 8 < 10 
	et a > 5 alors
		écrire("mdr")
	fsi
	
	écrire(a)
	écrire(b)
	
FIN 