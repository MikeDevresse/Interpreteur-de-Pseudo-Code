ALGORITHME Test1
constante:

variable:
	a : entier
	b : entier
	tmp : entier
	
DEBUT
	a <-- 1
	b <-- 10

	écrire(a) //écrire OUI si tq ecrire("")!!!!!!!!!!!!!!!!!
	écrire(b)
	
	tmp <-- a
	a <-- b //zqihdkzqgdukyqzgdukyzqfdukyzqfdukyzfqd
	b <-- tmp//commentaire
    //test commentaire () if
	écrire("a © b bonjour" © a)
	écrire(b)
FIN 