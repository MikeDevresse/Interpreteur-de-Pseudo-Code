ALGORITHME TestBoucle2
constante:
variable:
	i 	: entier
	j : entier
DEBUT
	i <-- 0
	j <-- 0
	
	i <-- i+1
	i <-- i+1
	ecrire(i)
	
	si 1 = 1 alors
		ecrire("simple")
	fsi
	
	si i < 0 alors
		ecrire("ok1")
		si i < 10 alors
			ecrire("ok2")
		fsi
	sinon
		ecrire("lol")
	fsi
	
	
FIN